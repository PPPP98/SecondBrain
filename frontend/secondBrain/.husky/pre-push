#!/usr/bin/env sh

# Load Husky environment
. "$(dirname "$0")/_/husky.sh"

# Frontend 디렉토리로 이동
cd frontend/secondBrain

echo "🏗️  빌드 실행 중..."
pnpm run build

if [ $? -ne 0 ]; then
  echo "❌ 빌드 실패했습니다. 에러 수정 후 다시 push해주세요."
  exit 1
fi

echo "✅ 빌드 성공!"
